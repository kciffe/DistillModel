## 第三章 【掌握】模型训练

### 1、学习目标

训练模型三重点：

训练方式、基座模型、工具

### 2、LoRA微调

#### 2.1 什么是LoRA微调

LoRA微调的全程是Low-Rank Adaptation，这种方法通过引入低秩矩阵分解，可以通过仅训练少量参数实现大模型的适配。

#### 2.2 全参数微调的缺陷

全参数微调有几个重大的缺陷

##### 2.2.1 训练成本高

全参数微调，需要更新千亿甚至万亿级别的参数，会消耗大量计算资源，为了调整海量参数时不跑偏，还需要准备相当规模的数据集，费时费力。

##### 2.2.2 训练时间长

全参数微调训练时间会比较长，有些场合需要快速迭代，跟不上业务的节奏

##### 2.2.3 灾难性遗忘

全参数微调需要根据新任务，调整和重构预训练模型的所有参数，有可能导致原有的知识表征被破坏，导致新任务上表现虽好，但是旧任务上的表现急剧下降，这就是大模型的灾难性遗忘。

#### 2.3 LoRA微调怎样避免这些问题？

需要训练一个巨大的参数矩阵$W_{M*N}$，为了避免灾难性遗忘，训练的时候，我们不能调整原始矩阵，而是要调整一个同样大小的矩阵$ΔW_{M*N}$，使得推理时可以用$W_{M*N}+αΔW_{M*N}$代替原来的$W_{M*N}$。

此时训练的参数量仍未M*N，如何改善？

机器学习里的PCA使用过一个思路：矩阵分解。很多算法都使用类似的方法降低计算量（潜在语义分析LSA，推荐系统里的协同过滤）

![LoRA原理](.\..\resources\LoRA原理.png)

不是真正地训练一个巨大的矩阵$ΔW_{M*N}$，而是训练两个小型矩阵$A_{M*r}$和$B_{r*N}$，使得$A_{M*r}*B_{r*N}≈ΔW_{M*N}$，只要这两个结果足够近似，在工程上就是可行的。

如果r远小于M 、N，就可以极大地压缩参数量。

假设M=300000，N=500000，那么$ΔW_{M*N}$的总参数量为M*N=1500亿。

如果取r=16，那么$A_{M*r}$的参数量为M*k=480万，

​                        同理$B_{r*N}$的参数量为k*N=800万，

​                        总参数量为1280万，仅为$ΔW_{M*N}$总参数量的0.000085，即不到万分之一。

#### 2.4 实际做法

- 每个transformer都添加低秩旁路
- 矩阵A高斯初始化
- 矩阵B初始化为全0矩阵

![LoRA](.\..\resources\LoRA.png)

#### 2.4 小结

> 问题：1. 使用全参数微调有什么缺点？
>
> ​            2. LoRA微调为什么能避免灾难性遗忘？
>
> ​            3. LoRA微调为什么能减小训练参数量？
>
> ​            4. LoRA的A，B矩阵是怎样初始化的？为什么不能全部初始化为0？为什么不能全部做高斯初始化？

### 3、获取基座模型

科学上网的同学可以去huggingface的模型库

如果无法科学上网可以通过魔搭社区获取

在这里https://www.modelscope.cn/models搜索想要的模型

![搜索模型](.\..\resources\搜索模型.jpg)

这是模型的介绍，点击红圈里的“模型文件”tab页可以进入下载页面

![模型文件](.\..\resources\模型文件.jpg)

这是模型的下载页面，点击红圈里的“下载模型”可以得到下载模型的命令

![下载模型的tab页](.\..\resources\下载模型的tab页.jpg)

红圈里是下载模型的命令

![下载模型的命令](.\..\resources\下载模型的命令.jpg)

使用--local_dir指定本地存储位置

```bash
modelscope download --model Qwen/Qwen2.5-3B-Instruct-GPTQ-Int4 --local_dir /workspace/deepseekDistllation/models/Qwen/Qwen2.5-3B-Instruct-GPTQ-Int4
```

#### 3.1 小结

> 实操：获取一个小的基座模型并在本机找到模型文件

### 4、腾讯cloud studio

#### 4.1 注册和登录

网址：https://cloudstudio.net/

![CS注册1](.\..\resources\CS注册1.png)

可以使用微信注册和登录

![CS注册2](.\..\resources\CS注册2.png)

#### 4.2 找到共享的项目

可以在首页上找到老师分享的项目

![找到分享的项目](.\..\resources\找到分享的项目.png)

也可以在个人信息里找到分享的项目

![个人信息](.\..\resources\个人信息.png)

#### 4.3 使用共享项目

点击运行启动虚拟机

![点击运行](.\..\resources\点击运行.png)

点击 终端/代码 可以在终端和代码中切换，点击停止可以关闭虚拟机

![切换代码和终端](.\..\resources\切换代码和终端.png)

#### 4.4 小结

>实操：登录到腾讯cloud studio，找到并进入共享项目 deepseek distillation

### 5、使用训练工具LLaMA-Factory

#### 5.1 LLaMA-Factory简介

实际工作时，与学习时有一定的差异，会用一些工具提升工作效率

LLaMA-Factory 是一个封装比较完善的LLM微调工具，可以仅使用命令行与webui微调大模型，对微调工作提供了各种支持。

多种模型：LLaMA、LLaVA、Mistral、Mixtral-MoE、Qwen、Qwen2-VL、DeepSeek、Yi、Gemma、ChatGLM、Phi 等等100+。

集成方法：（增量）预训练、（多模态）指令监督微调、奖励模型训练、PPO 训练、DPO 训练、KTO 训练、ORPO 训练等等。

多种精度：16 比特全参数微调、冻结微调、LoRA 微调和基于 AQLM/AWQ/GPTQ/LLM.int8/HQQ/EETQ 的 2/3/4/5/6/8 比特 QLoRA 微调。

先进算法：GaLore、BAdam、APOLLO、Adam-mini、DoRA、LongLoRA、LLaMA Pro、Mixture-of-Depths、LoRA+、LoftQ 和 PiSSA。

git地址：https://github.com/hiyouga/LLaMA-Factory

#### 5.2 环境搭建

##### 5.2.1 创建虚拟环境

```
conda create --name llamafactory-3.11 python==3.11

conda activate llamafactory-3.11 
```

##### 5.2.2 拉代码安装

```
git clone --depth 1 https://github.com/hiyouga/LLaMA-Factory.git

cd LLaMA-Factory

#pip install -e ".[torch,metrics]"

如果报错：No matching distribution found for setuptools>=61.0
pip install -r requirements.txt -i https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple
pip install --no-build-isolation --no-index --find-links=./ --no-deps -e ".[torch,metrics]"
```

##### 5.2.3 追加两个包

```
pip install auto_gptq
pip install optimum
```

#### 5.3 LLaMA-Factory的使用

##### 5.3.1 使用webui

在Anaconda Prompt中输入以下命令

```
conda activate llamafactory-3.11
llamafactory-cli webui

如果报错：argument of type 'bool' is not iterable
pip install pydantic==2.10.6
```

本地操作系统，启动后在浏览器上输入 http://localhost:7860，进入webui页面

![webui页面](C:/Users/foxba/Documents/工作/deepseek蒸馏小模型/resources/webui页面.jpg)

![webui页面2](C:/Users/foxba/Documents/工作/deepseek蒸馏小模型/resources/webui页面2.jpg)

腾讯cloudstudio：https://0da1d66dc78045a7afdebbc51a2c93a1--7860.ap-shanghai.cloudstudio.club

![腾讯cloudstudio启动训练界面](.\..\resources\腾讯cloudstudio启动训练界面.png)

![腾讯cloudstudio的训练界面](.\..\resources\腾讯cloudstudio的训练界面.png)



##### 5.3.2 使用命令行训练

```
llamafactory-cli train \
    --stage sft \
    --do_train True \
    --model_name_or_path /workspace/deepseekDistllation/models/Qwen/Qwen2.5-3B-Instruct-GPTQ-Int4 \
    --preprocessing_num_workers 16 \
    --finetuning_type lora \
    --template qwen \
    --flash_attn auto \
    --dataset_dir /workspace/deepseekDistllation/data/chatData \
    --dataset chat-train \
    --cutoff_len 6000 \
    --learning_rate 0.0001 \
    --num_train_epochs 1.0 \
    --max_samples 100000 \
    --per_device_train_batch_size 2 \
    --gradient_accumulation_steps 2 \
    --lr_scheduler_type cosine \
    --max_grad_norm 1.0 \
    --logging_steps 5 \
    --save_steps 100 \
    --warmup_steps 10 \
    --packing False \
    --report_to none \
    --output_dir /workspace/deepseekDistllation/models/lora/Qwen2.5-3B-instruct-GPTQ-Int4/train_2025-04-22-07-55-05 \
    --bf16 True \
    --plot_loss True \
    --trust_remote_code True \
    --ddp_timeout 180000000 \
    --include_num_input_tokens_seen True \
    --optim adamw_torch \
    --quantization_bit 4 \
    --quantization_method bitsandbytes \
    --double_quantization True \
    --lora_rank 16 \
    --lora_alpha 16 \
    --lora_dropout 0 \
    --lora_target all
```

#### 5.4 小结

>实操：1. 搭建LLaMA-Factory环境
>
>​            2.启动webui
>

### 5、训练过程

#### 5.1 指定基座模型

模型名称：Qwen2.5-3B-Instruct-GPTQ-Int4

模型路径：/workspace/deepseekDistllation/models/Qwen/Qwen2.5-3B-Instruct-GPTQ-Int4

检查点路径：

#### 5.2 指定数据集

数据路径：/workspace/deepseekDistllation/data/chatData

数据集：chat-train

#### 5.3 指定训练的基本性质

微调方法：LoRA

量化等级：4

训练阶段：Supervised fine-tuning

#### 5.4 指定重要参数

学习率：1e-4  或者 5e-5

截断长度：6000

训练轮数：1.0

批处理大小：1

梯度累计：2

预热步数：10

LoRA的秩：8

LoRA缩放系数：16

LoRA随机丢弃：0.1

#### 5.5 指定训练完成后的模型存储位置

/workspace/deepseekDistllation/models/lora/Qwen2.5-3B-instruct-GPTQ-Int4/

#### 5.6 开始训练

点击开始按钮

#### 5.7 小结

>实操：启动webui，设置好各类参数，启动一次训练，并检查模型文件和参数文件

### 6、 检查训练效果

#### 6.1 加载基座模型

```shell
CUDA_VISIBLE_DEVICES=0 API_PORT=8000 llamafactory-cli api \
    --model_name_or_path /workspace/deepseekDistllation/models/Qwen/Qwen2.5-3B-Instruct-GPTQ-Int4
```

#### 6.2 加载基座模型+LoRA

```shell
CUDA_VISIBLE_DEVICES=0 API_PORT=8000 llamafactory-cli api \
    --model_name_or_path /workspace/deepseekDistllation/models/Qwen/Qwen2.5-3B-Instruct-GPTQ-Int4 \
    --adapter_name_or_path /workspace/deepseekDistllation/models/lora/Qwen2.5-3B-instruct-GPTQ-Int4/train_2025-03-20-20-41-50/checkpoint-1250 \
    --finetuning_type lora
```

在腾讯cloudstudio上启动

![cloudstudio上启动llama-api服务](.\..\resources\cloudstudio上启动llama-api服务.png)

如果见到下面的信息，就代表api服务已经启动完毕

![cloudstudio上启动llama-api完毕](.\..\resources\cloudstudio上启动llama-api完毕.png)

#### 6.3 使用代码访问服务

可以用代码访问部署好的api server

```
def callServer(input):
    port = 8000
    client = OpenAI(
        api_key="0",
        base_url="http://localhost:{}/v1".format(os.environ.get("API_PORT", port)),
    )
    messages = []
    messages.append({"role": "user", "content": input})
    result = client.chat.completions.create(messages=messages, model="test",max_tokens=6000)
    return result.choices[0].message.content
    
if __name__ == "__main__":
    input = "对于「初三女生在搀扶跌倒老奶奶后反被冤枉，但仍选择资助她千元」的新闻事件，你有什么看法？"
    a = time.time()
    result = callServer(input)
    b = time.time()
    print("输出长度："+str(len(result))+"字  耗时："+str(b-a)+"秒  输出速度："+str(len(result)/(b-a))+"字/秒")
    print(result)
    
```

访问时，可以观察显卡的实时状态，windows在命令行里启动nvidia-smi -l 1，linux 使用 watch -n 1 nvidia-smi

![查看显卡运行状态](.\..\resources\查看显卡运行状态.png)

测试问题："对于「初三女生在搀扶跌倒老奶奶后反被冤枉，但仍选择资助她千元」的新闻事件，你有什么看法？"

> 输出长度：1902字    耗时：376.63秒    输出速度：5.05 字/秒
>
> <think>
> 嗯，用户问的是关于“初三女生搀扶跌倒老奶奶后反被冤枉，但仍选择资助她千元”的新闻事件的看法。首先，我需要理解这个事件的核心，也就是事件的背景、双方的立场，以及事件的结果。可能用户想知道事件背后的道德问题，或者社会对这类事件的看法。
>
> 接下来，我应该考虑用户可能的背景。他们可能对社会伦理问题比较感兴趣，或者关注类似新闻事件的处理方式。用户可能是家长、学生，或者普通读者，想要了解事件中的道德选择和法律问题。
>
> 然后，我需要分析事件本身。初三女生搀扶老奶奶，但被冤枉，可能是因为有人看到她搀扶老奶奶，然后又离开，导致误会。她选择资助老奶奶，这可能是因为她没有得到应有的尊重，或者出于道德感。需要考虑的是，这种行为是否值得赞扬，是否能改变事件的处理方式。
>
> 还要考虑法律和道德的平衡。虽然法律可能无法完全解决问题，但道德上的处理可能对事件有影响。用户可能希望了解法律是否介入，或者是否应该通过法律手段解决问题。
>
> 另外，用户可能想知道类似事件的处理方式，或者这种行为在社会中的普遍性。是否需要更多的人站出来，或者是否应该改变对这类行为的处理方式。可能需要探讨公众的反应，以及媒体的报道方式是否合理。
>
> 还要考虑事件中的其他因素，比如老奶奶的情况，是否真的需要帮助，或者是否有人故意诬陷。但用户的问题没有涉及这些细节，所以可能不需要深入讨论，但可以简要提及。
>
> 最后，总结观点，强调道德的重要性，社会的尊重和责任，以及媒体和法律的作用。同时，可能需要建议公众如何正确处理类似事件，或者呼吁对这类事件的公正处理。
>
> </think>
>
> 
>
> 关于“初三女生搀扶跌倒老奶奶后反被冤枉，但仍选择资助她千元”的事件，可以从以下几个角度进行思考：
>
> ### 一、事件的核心价值与道德选择
>
> 1. **道德选择的合理性**  
>    - 女生在搀扶老奶奶后主动资助其千元，体现了“善良”与“正义”的双重价值。这种行为超越了简单地“助人”，更传递了“不因误解而放弃责任”的精神。
>    - 在道德层面，这种选择往往能引发社会对“诚信”“尊重”的反思，有助于塑造社会向善的氛围。
>
> 2. **法律与道德的博弈**  
>    - 事件中的“冤枉”可能源于法律与道德的界限模糊。法律通常关注事实真相，而道德则强调“心怀善意”的价值。当法律无法完全还原真相时，道德选择往往成为最后的救赎方式。
>
> ### 二、事件的深层反思
>
> 1. **社会信任的重构**  
>    - 这个事件可能引发公众对“诚信”“尊重”的再思考。当社会对“善意”的怀疑加深，个体的道德选择反而成为维系社会信任的关键。
>
> 2. **媒体与舆论的角色**  
>    - 新闻报道中，媒体对“冤枉”事件的过度渲染可能加剧公众的负面情绪，但同时，对事件的积极报道也展示了社会对正义的渴望。这种“反转”的新闻价值，本身可能成为社会进步的催化剂。
>
> 3. **制度与法律的完善**  
>    - 这类事件暴露了法律体系在处理“道德争议”时的局限性。例如，法律难以完全覆盖“好人”被诬陷的情况，这可能促使社会法律体系在伦理责任方面进行完善。
>
> ### 三、对社会的启示
>
> 1. **公众责任的提升**  
>    - 女生的资助行为不仅改变了事件的走向，也提醒公众：当遇到道德困境时，个体的善行可能比法律更具影响力。这种“道德补救”需要社会机制的支持，例如公众的监督、法律的宽容。
>
> 2. **法律与道德的平衡**  
>    - 事件可能促使社会对“法律”与“道德”的边界重新定义。法律不应过度介入道德纠纷，但道德行为的法律化可能成为未来社会发展的方向。
>
> 3. **尊重与责任的传承**  
>    - 事件中的老奶奶被资助，可能激发社会对“被尊重”的重视。这种行为不仅改变了事件本身，也传递了“被尊重”的价值，可能成为社会文明进步的隐喻。
>
> ### 四、可能的建议
>
> 1. **加强公共道德教育**  
>    - 通过教育引导公众在面对道德困境时，能够理解“善良”的价值，避免因误解而放弃善意。
>
> 2. **完善法律与社会机制**  
>    - 建立“道德补偿机制”，当法律无法完全还原真相时，社会通过制度化方式对“善行”给予支持。
>
> 3. **媒体与公众的角色**  
>    - 媒体应引导公众理性看待事件，避免过度渲染负面情绪，同时鼓励公众对道德行为的记录和传播。
>
> ### 结语
>
> 事件本身没有对错，但其结果展现了社会对“善意”的认知和尊重。当“好人”被冤枉时，选择资助老奶奶的女生用行动证明了“善良”的价值。这种行为不仅是对事件的救赎，更是对社会伦理的重新定义。



#### 6 小结

>实操：启动api server，并使用代码访问，注意观察原版模型和训练后模型输出结果的不同



### 7、主要参数

LLaMA-Factory训练过程中用到的参数非常多，按照重要程度整理如下：

#### 7.1 极重要，本次训练模型和数据路径

| 参数名称     | 说明                                                         |
| ------------ | ------------------------------------------------------------ |
| 模型名称     | 基座模型的名称                                               |
| 模型路径     | 基座模型的路径                                               |
| 检查点路径   | 开始的checkpoint路径，如果不需要可以为空                     |
| 数据路径     | 数据存放的目录                                               |
| 数据集       | dataset_info.json中记录的哪一个数据集                        |
| 输出目录     | 训练好的模型的路径，windows下默认在 C:\Users\foxba\saves\Qwen2.5-3B-Instruct-GPTQ-Int4\LoRA\ |
| 训练参数目录 | C:\Users\foxba\config\                                       |
| 对话模板     | 和使用的基座模型一致                                         |

#### 7.2 极重要，决定本次训练的基本性质

| 参数名称 | 说明                                                         |
| -------- | ------------------------------------------------------------ |
| 微调方法 | full--全参数训练  <br />freeze--冻结一部分参数<br />LoRA--使用LoRA微调 |
| 训练阶段 | 属于哪个训练阶段<br />Supervised Fine-Tuning：进一步做有监督训练<br />Reward Modeling：奖励建模，用于强化学习<br />PPO：强化学习算法之一，用于增强多次迭代中的稳定性<br />DPO：强化学习算法之一，用于避免策略和价值函数之间的相互影响，提高学习效率<br />KTO：知识转移方法，可以强化学习效率和最终效果<br />Pre-Training：预训练<br />本次实验选Supervised Fine-Tuning |
| 量化等级 | none：不做量化<br />8：8位量化<br />4：4位量化               |

#### 7.3 重要，决定本次训练的整体效果

| 参数名称     | 说明                                                         |
| ------------ | ------------------------------------------------------------ |
| 学习率       | 0.1       一般只用于探索，没人会真的用<br />0.01      从头训练的标准模型初始学习率<br />0.001    已经快接近优化目标时做细致调整时采用<br />0.0001  模型接近收敛时做微调<br />0.00005  预训练阶段的最后微调 |
| 截断长度     | 本次实验选6000                                               |
| 训练轮数     | 学习的特性较简单 ，1轮即可，如果数据集太小，不应选择太多轮数，可以考虑数据增强。 |
| 批处理大小   | 根据显存的情况调整                                           |
| 梯度累计     | 批处理大小*梯度累计决定了梯度更新的频率                      |
| 预热步数     | 建议使用，防止参数剧烈震荡                                   |
| LoRA的秩     | 学到信息的丰富程度                                           |
| LoRA缩放系数 | LoRA的重要程度                                               |
| LoRA随机丢弃 | 随机丢弃LoRA层权重的概率，是一种正则化方法，方式模型过度依赖LoRA |

#### 7.4 小结

> 问题：1. 有哪两类参数是做任何训练都必须设置的
>
> ​            2.我想学习电气自动化的数据集，共有1500条，使用了0.01的学习率，做了10轮训练，为什么效果不好？
>
> ​             3.启动训练过程后不久即停止，日志里最后的错误如下：
>
> ​                torch.OutOfMemoryError:CUDA out of memory,Tried to allocate 7.26G，GPU 0 has a total capacity of 23.64GB which 16.56MB is free
>
> ​                 问题可能出在哪里？怎么解决？
